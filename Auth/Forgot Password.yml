info:
  name: Forgot Password
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/forgot-password"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  body:
    type: json
    data: |-
      {
        "email": "echanojanpatrick@gmail.com"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
