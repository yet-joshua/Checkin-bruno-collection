info:
  name: Get user
  type: http
  seq: 4

http:
  method: GET
  url: "{{checkin}}/api/v1/user"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
