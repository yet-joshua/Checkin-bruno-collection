info:
  name: Register
  type: http
  seq: 1

http:
  method: POST
  url: "{{checkin}}/api/v1/register"
  body:
    type: json
    data: |-
      {
        "email": "philiprizzler@example.com",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
