info:
  name: Review Organizers
  type: http
  seq: 5

http:
  method: GET
  url: "{{baseUrl}}/hosts/{admin_id}/reviews"
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
