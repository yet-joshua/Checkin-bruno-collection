info:
  name: Popular Organizers
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/hosts"
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
