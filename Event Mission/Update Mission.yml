info:
  name: Update Mission
  type: http
  seq: 3

http:
  method: PUT
  url: "{{checkin}}/api/v1/events/{event_id}/missions/{mission_id}"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  body:
    type: json
    data: |
      {
        "title": "Find the Secret VIP Lounge"
      }
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
