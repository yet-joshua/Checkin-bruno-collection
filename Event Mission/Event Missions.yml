info:
  name: Event Missions
  type: http
  seq: 7

http:
  method: GET
  url: "{{checkin}}/api/v1/user/personal-details"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
