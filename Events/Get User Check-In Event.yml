info:
  name: Get User Check-In Event
  type: http
  seq: 5

http:
  method: GET
  url: "{{checkin}}/api/v1/events/2/checkins"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
