info:
  name: Event List
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/events"
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
