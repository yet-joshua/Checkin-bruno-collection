info:
  name: Event Details
  type: http
  seq: 3

http:
  method: GET
  url: "{{checkin}}/api/v1/events/019d2991-10cf-73e8-8faa-417cf7697230"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
