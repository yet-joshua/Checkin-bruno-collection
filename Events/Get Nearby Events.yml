info:
  name: Get Nearby Events
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/events/nearby"
  params:
    - name: latitude
      value: "14.5995"
      type: query
    - name: longitude
      value: "120.9842"
      type: query
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
