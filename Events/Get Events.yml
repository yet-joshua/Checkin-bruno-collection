info:
  name: Get Events
  type: http
  seq: 2

http:
  method: GET
  url: "{{checkin}}/api/v1/events"
  headers:
    - name: Content-Type
      value: application/json
    - name: Accept
      value: application/json
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
