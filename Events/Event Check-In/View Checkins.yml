info:
  name: View Checkins
  type: http
  seq: 1

http:
  method: GET
  url: "{{checkin}}/api/v1/events{event_id}/checkins"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
