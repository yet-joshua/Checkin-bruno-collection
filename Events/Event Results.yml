info:
  name: Event Results
  type: http
  seq: 6

http:
  method: GET
  url: "{{baseUrl}}/events/{event_id}/results"
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
