info:
  name: Delete Event Winners
  type: http
  seq: 4

http:
  method: DELETE
  url: "{{checkin}}/api/v1/event_id/{event_id}/winners{winner_id}"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
