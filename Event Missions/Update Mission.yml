info:
  name: Update Mission
  type: http
  seq: 3

http:
  method: PUT
  url: "{{baseUrl}}/events/{event_id}/missions/{mission_id}"
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |
      {
        "title": "Find the Secret VIP Lounge"
      }
  auth:
    type: bearer
    token: "{{admin_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
