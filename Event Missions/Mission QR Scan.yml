info:
  name: Mission QR Scan
  type: http
  seq: 5

http:
  method: POST
  url: "{{baseUrl}}/missions/scan"
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |
      {
        "event_id": "9a1b2c3d-4e5f...",
        "qr_trigger_token": "a1b2c3d4e5f6g7h8..."
      }
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
