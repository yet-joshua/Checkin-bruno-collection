info:
  name: View Checkins
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/admin/events/{event_id}/checkins"
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  auth:
    type: bearer
    token: "{{admin_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
