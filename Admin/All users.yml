info:
  name: All users
  type: http
  seq: 11

http:
  method: GET
  url: "{{baseUrl}}/admin/users"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
