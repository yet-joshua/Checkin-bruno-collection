info:
  name: Update Event
  type: http
  seq: 4

http:
  method: PUT
  url: "{{baseUrl}}/admin/events/{event_id}"
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |
      {
        "title": "Summer Festival 2024",
        "location_name": "Central Park",
        "polygon_coordinates": [
          {"lat": 14.5, "lng": 121.0},
          {"lat": 14.6, "lng": 121.0},
          {"lat": 14.6, "lng": 121.1},
          {"lat": 14.5, "lng": 121.1}
        ]
      }
  auth:
    type: bearer
    token: "{{admin_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
