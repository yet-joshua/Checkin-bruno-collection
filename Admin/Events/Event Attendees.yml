info:
  name: Event Attendees
  type: http
  seq: 8

http:
  method: GET
  url: "{{baseUrl}}/admin/events/{event_id}/attendees"
  params:
    - name: status
      value: registered
      disabled: true
    - name: search
      value: ""
      disabled: true
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  auth:
    type: bearer
    token: "{{admin_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
