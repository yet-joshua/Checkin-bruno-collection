info:
  name: Notifications
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/user/notifications"
  params:
    - name: per_page
      value: "15"
      type: query
    - name: page
      value: "1"
      type: query
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
