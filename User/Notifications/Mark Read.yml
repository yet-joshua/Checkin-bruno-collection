info:
  name: Mark Read
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{baseUrl}}/user/notifications/{notification_id}/read"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
