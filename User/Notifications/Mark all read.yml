info:
  name: Mark All Read
  type: http
  seq: 4

http:
  method: PATCH
  url: "{{baseUrl}}/user/notifications/read-all"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
