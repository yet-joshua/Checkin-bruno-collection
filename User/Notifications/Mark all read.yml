info:
  name: Mark all read
  type: http
  seq: 3

http:
  method: PATCH
  url: "{{checkin}}/api/v1/user/notifications/read-all"
  body:
    type: json
    data: ""
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
