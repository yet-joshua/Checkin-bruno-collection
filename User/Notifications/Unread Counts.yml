info:
  name: Unread Counts
  type: http
  seq: 2

http:
  method: GET
  url: "{{checkin}}/api/v1/user/notifications/unread-count"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
