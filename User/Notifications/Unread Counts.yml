info:
  name: Unread Counts
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/user/notifications/unread-count"
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
