info:
  name: User Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{checkin}}/api/v1/login"
  body:
    type: json
    data: |
      {
          "contact_number": "+639123456789", 
          "password": "MySecretPassword123!"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const response = res.getBody();
        bru.setEnvVar("auth_token", response.data.token);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
