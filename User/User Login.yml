info:
  name: User Login
  type: http
  seq: 10

http:
  method: POST
  url: "{{baseUrl}}/auth/login"
  body:
    type: json
    data: |
      {
        "contact_number": "+639171000001",
        "password": "password"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
