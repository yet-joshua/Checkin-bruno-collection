info:
  name: Logout
  type: http
  seq: 4

http:
  method: POST
  url: "{{checkin}}/api/v1/logout"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
