info:
  name: User List
  type: http
  seq: 5

http:
  method: GET
  url: "{{checkin}}/api/v1/user"
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{checkin}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
