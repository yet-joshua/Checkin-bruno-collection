info:
  name: Pre-register
  type: http
  seq: 6

http:
  method: POST
  url: "{{checkin}}/api/v1/validate-registration"
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: |
      {
          "contact_number": "+639211234567",
          "password": "Password123!",
          "password_confirmation": "Password123!"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
