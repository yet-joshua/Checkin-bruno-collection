info:
  name: User Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{checkin}}/api/v1/auth/login"
  body:
    type: json
    data: |-
      {
        "contact_number": "+639956421817",
        "password" : "password"
      }
  auth: inherit

runtime:
  scripts:
    - type: after-response
      code: |-
        const response = res.getBody();
        bru.setEnvVar("auth_token", response.data.token);

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
