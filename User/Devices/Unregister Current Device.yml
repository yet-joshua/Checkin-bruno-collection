info:
  name: Unregister Current Device
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{baseUrl}}/user/devices/current"
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5