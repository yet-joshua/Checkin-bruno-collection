info:
  name: Delete User
  type: http
  seq: 3

http:
  method: DELETE
  url: "{{checkin}}/api/v1/delete/"
  headers:
    - name: Accept
      value: application/json
    - name: Content-Type
      value: application/json
  body:
    type: json
    data: ""
  auth:
    type: bearer
    token: "{{auth_token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
