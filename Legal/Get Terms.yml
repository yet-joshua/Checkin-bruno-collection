info:
  name: Get Legal Terms
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/legal/terms"
  headers:
    - name: Accept
      value: application/json

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
